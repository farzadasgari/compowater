# Notebooks

Exploratory analysis lives here. Numbered by pipeline stage.

## Conventions
- Notebooks are for exploration only. Any logic that proves reusable
  gets promoted into `src/compowater/` as a proper module/function.
- These `.ipynb` files are intentionally **not tracked by git** right
  now (see `.gitignore`). They
  will be added to version control once the manuscript is ready for
  open-access release.